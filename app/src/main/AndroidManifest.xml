<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.click_labs.kashishnalwa.schooappproject">

    <application
        android:allowBackup="true"
        android:icon="@drawable/school_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewPagerFragment" />
        <activity android:name=".LoginPage" />
        <activity android:name=".Class_Information" />
        <activity android:name=".ViewPagerFragment1" />
        <activity android:name=".StudentData" />
        <activity android:name=".SettingsPage"></activity>
    </application>

</manifest>
